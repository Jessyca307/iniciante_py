"""
for + range
range -> range(start,stop, step)
interador -> str, range, etc
next -> entregue o proximo valor
iter -> me entregue seu interador
numeros = range(0, 100, 8)

for numero in numeros:
          print(numero)
"""
texto = 'Luiz'

#interpretador = iter(texto)

#while True:
#          try:
#                  letra = next(interpretador)
#                  print(letra)
#          except StopIteration:
 #                 break


for letra in texto:
          print(letra)